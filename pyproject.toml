[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ips_sw"
version = "0.1.0"
description = "Code for Hardik Medhi's PhD Thesis"
dependencies = []

[tool.setuptools.package-data]
ips_sw = ["yaml_info/*", "matplotlib_styles/*"]